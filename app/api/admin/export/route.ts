import { NextResponse } from "next/server";
import { getSession } from "@/lib/auth";
import { getExportRows } from "@/lib/submissions";
import * as XLSX from "xlsx";
export async function GET(){if(!(await getSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const rows=await getExportRows();const sheet=XLSX.utils.json_to_sheet(rows.map(row=>({"Full Name":row.name,"Phone Number":row.phone,"Instagram Username":row.instagram||"","LinkedIn Profile":row.linkedin||"","Interested in Free Video":row.interestedInFreeVideo?"Yes":"No","Email":row.email,"Submission Date":row.createdAt,"Submission ID":row.id})));const workbook=XLSX.utils.book_new();XLSX.utils.book_append_sheet(workbook,sheet,"Submissions");const buffer=XLSX.write(workbook,{type:"buffer",bookType:"xlsx"});return new NextResponse(buffer,{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":`attachment; filename="submissions-${new Date().toISOString().slice(0,10)}.xlsx"`}});}